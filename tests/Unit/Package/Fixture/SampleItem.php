<?php

declare(strict_types=1);

namespace ModxKit\Testbench\Tests\Unit\Package\Fixture;

/**
 * A stub for a model class of an extra: `PackageDefinition::tables()` accepts a `class-string`,
 * so the tests need classes that really exist rather than invented names.
 */
final class SampleItem
{
}
